# Copy to a real .env (or set as OS/IDE environment variables) - never commit real secrets.

DB_URL=jdbc:postgresql://localhost:5432/testbyte
DB_USERNAME=testbyte_app
DB_PASSWORD=testbyte_dev_pw

# Generate with: openssl rand -base64 48
JWT_SECRET=

# Zoho SMTP - use an app-specific password from Zoho Mail > Security > App Passwords
MAIL_HOST=smtp.zoho.com
MAIL_PORT=587
MAIL_USERNAME=ravitejananduri@zoho.com
MAIL_PASSWORD=
MAIL_FROM=ravitejananduri@zoho.com

FRONTEND_BASE_URL=http://localhost:4200
APPROVER_EMAIL_DEFAULT=ravitejananduri@zoho.com
